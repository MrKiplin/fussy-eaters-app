{
  "name": "react-electron-template",
  "version": "1.0.0",
  "description": "A simple template for integrating React into an Electron desktop app",
  "main": "index.js",
  "repository": "https://github.com/MrKiplin/react-electron-template.git",
  "author": "MrKiplin <tjworks@hotmail.co.uk>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build": "yarn lerna run build --stream",
    "lint": "yarn lerna run lint --stream",
    "start": "yarn lerna run start --stream",
    "test": "yarn lerna run test --stream"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
